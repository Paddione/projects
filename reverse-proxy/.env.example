# Traefik Configuration
TZ=Europe/Berlin

# Let's Encrypt
ACME_EMAIL=patrick@korczewski.de

# Traefik Dashboard
# Generate with: htpasswd -nb admin your-password (use the hash after the colon)
TRAEFIK_DASHBOARD_USER=admin
TRAEFIK_DASHBOARD_PASSWORD_HASH=$apr1$8jT7RHxC$7l4L5Ov0k5qY5Zz5w5w5w0

# Optional: Use staging Let's Encrypt for testing
# ACME_CA_SERVER=https://acme-staging-v02.api.letsencrypt.org/directory

# DNS-01 provider (ipv64.net)
IPV64_API_KEY=your-ipv64-api-key
