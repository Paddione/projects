{
  "name": "assetgenerator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Audio asset review and regeneration tool",
  "scripts": {
    "start": "node server.js --project arena",
    "dev": "node --watch server.js --project arena",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "free-tex-packer-core": "^0.3.5",
    "open": "^10.1.0",
    "tsx": "^4.7.0",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "vitest": "^4.1.0"
  }
}
