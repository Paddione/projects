# Generated assets (re-create via scripts/generate_all.sh)
assets/concepts/
assets/models/
assets/renders/
assets/audio/

# Environment variables (secrets)
.env

# Python virtual environment
.venv/

# Keep manifest, sprites, and compressed audio in git
!assets/manifest.json
!frontend/public/assets/
# Exclude raw WAV source files (large, regenerate via pipeline)
frontend/public/assets/**/*.wav
