# =============================================================================
# Skaffold Configuration
# =============================================================================
apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: korczewski-k8s

# =============================================================================
# Build Configuration
# =============================================================================
build:
  local:
    push: true
    useBuildkit: true
    concurrency: 2
  tagPolicy:
    sha256: {}

  artifacts:
    # Auth Service
    - image: registry.korczewski.de/korczewski/auth
      context: ..
      docker:
        dockerfile: auth/Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: /app/src

    # L2P Backend
    - image: registry.korczewski.de/korczewski/l2p-backend
      context: ..
      docker:
        dockerfile: l2p/backend/Dockerfile

    # L2P Frontend (URLs injected at runtime via docker-entrypoint.sh)
    - image: registry.korczewski.de/korczewski/l2p-frontend
      context: ..
      docker:
        dockerfile: l2p/frontend/Dockerfile

    # Shop Service
    - image: registry.korczewski.de/korczewski/shop
      context: ..
      docker:
        dockerfile: shop/Dockerfile

    # VideoVault
    - image: registry.korczewski.de/korczewski/videovault
      context: ..
      docker:
        dockerfile: VideoVault/Dockerfile.prod

    # SOS (Taschentherapeut)
    - image: registry.korczewski.de/korczewski/sos
      context: ..
      docker:
        dockerfile: SOS/Dockerfile

    # Arena Backend
    - image: registry.korczewski.de/korczewski/arena-backend
      context: ..
      docker:
        dockerfile: arena/backend/Dockerfile

    # Arena Frontend
    - image: registry.korczewski.de/korczewski/arena-frontend
      context: ..
      docker:
        dockerfile: arena/frontend/Dockerfile

# =============================================================================
# Manifest Configuration
# =============================================================================
manifests:
  kustomize:
    paths:
      - infrastructure/postgres
      - infrastructure/traefik
      - infrastructure/registry
      - infrastructure/smb-csi
      - infrastructure/cert-renewal
      - services/auth
      - services/l2p-backend
      - services/l2p-frontend
      - services/shop
      - services/videovault
      - services/sos
      - services/arena-backend
      - services/arena-frontend
  rawYaml:
    - base/namespaces.yaml

# =============================================================================
# Deploy Configuration
# =============================================================================
deploy:
  kubectl:
    hooks:
      before:
        - host:
            command:
              [
                "bash",
                "-c",
                "kubectl get ns korczewski-infra || kubectl apply -f base/namespaces.yaml",
              ]

# =============================================================================
# Port Forwarding
# =============================================================================
portForward:
  - resourceType: service
    resourceName: postgres
    namespace: korczewski-infra
    port: 5432
    localPort: 5432
  - resourceType: service
    resourceName: traefik-dashboard
    namespace: korczewski-infra
    port: 8080
    localPort: 8080
  - resourceType: service
    resourceName: auth
    namespace: korczewski-services
    port: 5500
    localPort: 5500
  - resourceType: service
    resourceName: l2p-backend
    namespace: korczewski-services
    port: 3001
    localPort: 3001
  - resourceType: service
    resourceName: l2p-frontend
    namespace: korczewski-services
    port: 80
    localPort: 3000
  - resourceType: service
    resourceName: shop
    namespace: korczewski-services
    port: 3000
    localPort: 3004
  - resourceType: service
    resourceName: videovault
    namespace: korczewski-services
    port: 5000
    localPort: 5100
  - resourceType: service
    resourceName: sos
    namespace: korczewski-services
    port: 3005
    localPort: 3005
  - resourceType: service
    resourceName: arena-backend
    namespace: korczewski-services
    port: 3003
    localPort: 3003
  - resourceType: service
    resourceName: arena-frontend
    namespace: korczewski-services
    port: 80
    localPort: 3002

# =============================================================================
# Profiles
# =============================================================================
profiles:
  # Auth only
  - name: auth
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/auth
          context: ..
          docker:
            dockerfile: auth/Dockerfile
    manifests:
      kustomize:
        paths:
          - services/auth

  # L2P only (backend + frontend)
  - name: l2p
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/l2p-backend
          context: ..
          docker:
            dockerfile: l2p/backend/Dockerfile
        - image: registry.korczewski.de/korczewski/l2p-frontend
          context: ..
          docker:
            dockerfile: l2p/frontend/Dockerfile
    manifests:
      kustomize:
        paths:
          - services/l2p-backend
          - services/l2p-frontend

  # Shop only
  - name: shop
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/shop
          context: ..
          docker:
            dockerfile: shop/Dockerfile
    manifests:
      kustomize:
        paths:
          - services/shop

  # VideoVault only
  - name: videovault
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/videovault
          context: ..
          docker:
            dockerfile: VideoVault/Dockerfile.prod
    manifests:
      kustomize:
        paths:
          - services/videovault

  # SOS only
  - name: sos
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/sos
          context: ..
          docker:
            dockerfile: SOS/Dockerfile
    manifests:
      kustomize:
        paths:
          - services/sos

  # Arena only (backend + frontend)
  - name: arena
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/arena-backend
          context: ..
          docker:
            dockerfile: arena/backend/Dockerfile
        - image: registry.korczewski.de/korczewski/arena-frontend
          context: ..
          docker:
            dockerfile: arena/frontend/Dockerfile
    manifests:
      kustomize:
        paths:
          - services/arena-backend
          - services/arena-frontend

  # Infrastructure only (no builds)
  - name: infra
    manifests:
      kustomize:
        paths:
          - infrastructure/postgres
          - infrastructure/traefik
          - infrastructure/registry
          - infrastructure/smb-csi
          - infrastructure/cert-renewal
      rawYaml:
        - base/namespaces.yaml

  # Development stack (parallel to production in korczewski-dev namespace)
  # Usage:
  #   skaffold dev -p dev     # Hot-reload, cleans up on Ctrl+C
  #   skaffold run -p dev     # Deploy and leave running
  #   skaffold delete -p dev  # Tear down dev stack
  - name: dev
    build:
      artifacts:
        - image: registry.korczewski.de/korczewski/auth
          context: ..
          docker:
            dockerfile: auth/Dockerfile
        - image: registry.korczewski.de/korczewski/l2p-backend
          context: ..
          docker:
            dockerfile: l2p/backend/Dockerfile
        - image: registry.korczewski.de/korczewski/l2p-frontend
          context: ..
          docker:
            dockerfile: l2p/frontend/Dockerfile
        - image: registry.korczewski.de/korczewski/shop
          context: ..
          docker:
            dockerfile: shop/Dockerfile
        - image: registry.korczewski.de/korczewski/videovault
          context: ..
          docker:
            dockerfile: VideoVault/Dockerfile.prod
        - image: registry.korczewski.de/korczewski/sos
          context: ..
          docker:
            dockerfile: SOS/Dockerfile
        - image: registry.korczewski.de/korczewski/arena-backend
          context: ..
          docker:
            dockerfile: arena/backend/Dockerfile
        - image: registry.korczewski.de/korczewski/arena-frontend
          context: ..
          docker:
            dockerfile: arena/frontend/Dockerfile
    manifests:
      kustomize:
        paths:
          - overlays/development
    deploy:
      kubectl:
        hooks:
          before:
            - host:
                command:
                  [
                    "bash",
                    "-c",
                    "kubectl get ns korczewski-dev >/dev/null 2>&1 || kubectl apply -f base/namespaces.yaml",
                  ]
    portForward:
      - resourceType: service
        resourceName: auth
        namespace: korczewski-dev
        port: 5500
        localPort: 5501
      - resourceType: service
        resourceName: l2p-backend
        namespace: korczewski-dev
        port: 3001
        localPort: 3101
      - resourceType: service
        resourceName: l2p-frontend
        namespace: korczewski-dev
        port: 80
        localPort: 3100
      - resourceType: service
        resourceName: shop
        namespace: korczewski-dev
        port: 3000
        localPort: 3104
      - resourceType: service
        resourceName: videovault
        namespace: korczewski-dev
        port: 5000
        localPort: 5101
      - resourceType: service
        resourceName: sos
        namespace: korczewski-dev
        port: 3005
        localPort: 3105
      - resourceType: service
        resourceName: arena-backend
        namespace: korczewski-dev
        port: 3003
        localPort: 3103
      - resourceType: service
        resourceName: arena-frontend
        namespace: korczewski-dev
        port: 80
        localPort: 3102
