# =============================================================================
# Cert Renewal Kustomization
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: cert-renewal

namespace: korczewski-infra

labels:
  - pairs:
      app.kubernetes.io/part-of: korczewski
      app.kubernetes.io/component: tls

resources:
  - rbac.yaml
  - cronjob.yaml
