# =============================================================================
# PgBouncer Admin Credentials
# =============================================================================
# Admin user for PgBouncer stats/monitoring (SHOW POOLS, SHOW STATS, etc.)
# Service user passwords are sourced from postgres-credentials via init container.
# =============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: pgbouncer-admin
  namespace: korczewski-infra
  labels:
    app: pgbouncer
    app.kubernetes.io/name: pgbouncer
    app.kubernetes.io/component: connection-pooler
    app.kubernetes.io/part-of: korczewski
    tier: infrastructure
type: Opaque
stringData:
  PGBOUNCER_ADMIN_PASSWORD: "0799ab98afcc9689541ebf23c6acbde3"
