---
# =============================================================================
# Shop Service IngressRoute
# =============================================================================

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: shop
  namespace: korczewski-services
  labels:
    app: shop
    app.kubernetes.io/name: shop
    app.kubernetes.io/component: shop
    app.kubernetes.io/part-of: korczewski
    tier: services
spec:
  routes:
    # Route for static assets (no auth required)
    - match: Host(`shop.korczewski.de`) && (PathPrefix(`/_next`) || PathPrefix(`/favicon.ico`) || PathPrefix(`/static`))
      kind: Rule
      services:
        - name: shop
          port: 3000
    # Route for everything else (requires auth)
    - match: Host(`shop.korczewski.de`)
      kind: Rule
      services:
        - name: shop
          port: 3000
      middlewares:
        - name: user-auth-chain
          namespace: korczewski-infra
  tls: {}
