# Development environment variables (loaded by Vite in dev mode)
# This ensures the frontend never redirects to production auth in development.

# Auth service URL for local development
# Set to empty string to disable external auth and use local JWT auth
# Set to http://localhost:5500 to use local auth service
VITE_AUTH_SERVICE_URL=http://localhost:5500
